/**
 * Initially this entity was supposed to be a plain JSON object stored in NoSQL document
 * But for now I don't have an API models I'll use for creating travel path
 */
// TODO: Have more presice types
export class PathData {
  [key: string]: unknown;
}
